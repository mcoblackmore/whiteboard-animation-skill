# Whiteboard Animation Skills

用于生成白板动画视频的可复用 Agent Skills 仓库。

当前仓库在 `skills/` 下包含两个 skill：

- `whiteboard-animation`：根据单张图片或多张图片批量生成白板手绘动画视频。
- `whiteboard-video-workflow`：从 SRT 字幕出发，完成分镜生成、图片生成、视频片段生成与最终合并的端到端工作流。